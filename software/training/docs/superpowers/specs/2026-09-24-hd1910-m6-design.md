# HD1910 M6 training baseline

The user confirmed that LuwuDynamics' 1910 is the same actuator used in this
replica and approved using its public M6 model, with real hardware testing later.

Add explicit Flat/Rough HD1910 walking tasks to the existing Microduck training
stack. Preserve the Microduck geometry, home pose, joint order, 61D observations,
14D actions, rewards, and 50 Hz policy rate. Use LuwuDynamics' parameter file
without changing its fitted values, plus its training actuator settings (P=5,
7.4–8.0 V, sag floor 7.0 V, delay lag 3–6).

Keep the XL330 tasks available as a comparison. Copy robot configuration before
changing actuators so registration cannot mutate other tasks. Give HD1910 runs
their own experiment directory. Record the source revision and Apache 2.0
attribution next to the parameter file.

The user explicitly requires attribution (2026-09-24): identify LuwuDynamics as
the source of the actuator parameters in the parameter directory, training guide,
and replica project documentation. Do not describe these as our own measurements.
Retain this attribution if we later train our own policy using the same model.

This is a simulation baseline. Existing Microduck masses/inertias are not newly
measured replica data. Do not substitute the XgoDuck 0.8 kg geometry, its fitted
zero offsets, its servo signs, or its pretrained policies for this robot's own
calibration. No motor commands, EEPROM writes, or deployment are part of this task.

Verify task registration, BAM parameter loading, configuration isolation, model
compilation and policy dimensions. Run the repository's 64-environment,
5-iteration local smoke test, then export that disposable checkpoint to ONNX.
Document remaining physical checks and make clear that a smoke checkpoint is not
a trained gait. Use local logging only.
