# HD1910 M6 Implementation Plan

> Execute inline in the isolated `codex/hd1910-m6` worktree using the existing
> approved scope. No additional design decision or hardware permission is needed.

**Goal:** A selectable, locally verified HD1910 walking simulation baseline.

**Architecture:** Derive Flat/Rough tasks from the existing velocity factory;
deep-copy its robot configuration and replace only the BAM actuator settings.
Store immutable upstream parameters with provenance and separate run names.

**Tech Stack:** Python 3.12, mjlab 1.3.0, BAM M6, MuJoCo Warp, PPO, ONNX.

- [x] Prepare a dependency environment and check existing configuration tests.
- [x] Add `tests/test_hd1910_cfg.py`; first demonstrate that the HD1910 task IDs
  do not exist. Cover BAM loading, 14 joint order, model/pose preservation,
  independent configurations, original XL330 configuration, and 50 Hz timing.
- [x] Add `robot/hd1910/1910_m6.json` from LuwuDynamics/xgoduck_rl revision
  `326d77a1122870bdefa2c36403937502c958e69c`, with a source README and checksum.
- [x] Add `tasks/microduck_hd1910_env_cfg.py`: call the existing velocity factory,
  copy `cfg.scene.entities["robot"]`, replace `articulation.actuators` with
  `FrictionDRBamActuatorCfg(json_path=..., kp_fw=5.0, vin_range=(7.4, 8.0),
  vin_drop_gain_range=(0.0, 0.2), vin_min=7.0, delay_min_lag=3, delay_max_lag=6)`.
  Derive the runner config with `dataclasses.replace`, experiment name
  `microduck_hd1910_velocity` and run name `hd1910_m6_luwu`.
- [x] Register `Mjlab-Velocity-{Flat,Rough}-MicroDuck-HD1910` in
  `tasks/__init__.py`, using `MicroduckOnPolicyRunner`.
- [x] Run configuration regression tests and a GPU environment rollout; verify
  finite 61D actor observations, 14D actions and BAM physics.
- [x] Run `train Mjlab-Velocity-Flat-MicroDuck-HD1910
  --env.scene.num-envs 64 --agent.max-iterations 5 --agent.logger tensorboard`;
  export its checkpoint with `scripts/export.py` and verify ONNX I/O and finite
  output. Record commands/results in `docs/hd1910-baseline.md`.
- [x] Add a README entry linking the usage guide, review the diff, and retain
  the finished local branch for subsequent real-hardware validation.

Execution finding: the pinned BAM Feetech controller has no `q_target_smooth`
outside log playback. The GPU regression reproduced this at first reset. Added
`actuator/feetech_bam.py` as a narrowly scoped subclass with deferred per-world
goal initialization/reset and direct history-isolation regression assertions.
26 relevant tests passed, the smoke run completed 7,680 transitions with no NaN
terminations, and the standard ONNX export passed model and inference checks.
Independent code review found no actionable correctness issues.
